

GSAP (GreenSock Animation Platform) é considerado o padrão da indústria para animações web profissionais:

- ⚡ **Performance superior** - 20x mais rápido que CSS animations
- 🎯 **Controlo preciso** - Frame-perfect animations
- 🔧 **ScrollTrigger poderoso** - Animações baseadas em scroll avançadas
- 📱 **Cross-browser** - Funciona em todos os browsers
- 🎨 **Flexibilidade total** - Anima qualquer propriedade CSS


Exemplo final:
![[gsap_scroll_examples.html]]

Ver exemplos no [codesandbox](https://codesandbox.io/p/sandbox/gsap-exemplos-2mmi6b)

## Exemplo slide fade

```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <style>
        body { margin: 0; font-family: Arial; height: 200vh; }
        .box { 
            width: 100px; 
            height: 100px; 
            background: #667eea; 
            margin: 50vh auto; 
        }
    </style>
</head>
<body>
    <div class="box"></div>
    
    <script>
        gsap.registerPlugin(ScrollTrigger);
        
        // Animação básica: fade in ao scroll
        gsap.from('.box', {
            opacity: 0,
            y: 100,
            duration: 1,
            scrollTrigger: '.box'
        });
    </script>
</body>
</html>
```

## Exemplo reveal cards

**conceitos:**

- `scrub` para animações atadas ao scroll
- `stagger` para delays escalonados
- `toggleActions` para controlo do comportamento
- `markers` para debugging

```html
<!DOCTYPE html>
<html>
  <head>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      }

      .hero {
        height: 100vh;
        background: #667eea;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 3rem;
      }
      .content {
        padding: 50px;
      }
      .card {
        width: 300px;
        height: 200px;
        background: white;
        margin: 50px auto;
        border-radius: 10px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
      }
    </style>
  </head>
  <body>
    <div class="hero">Hero Section</div>
    <div class="content">
      <div class="card">Card 1</div>
      <div class="card">Card 2</div>
      <div class="card">Card 3</div>
    </div>
    <script>
      gsap.registerPlugin(ScrollTrigger);

      // 2. Cards com animação staggered
      gsap.from(".card", {
        x: -100,
        opacity: 0,
        duration: 1,
        stagger: 0.2, // Delay entre cada elemento
        ease: "power2.out",
        scrollTrigger: {
          trigger: ".content",
          start: "top 80%",
          end: "bottom 20%",
          toggleActions: "play none none reverse",
        },
      });

      3. Animação com markers para debug
      gsap.from(".card", {
        scale: 0.5,
        rotation: 180,
        duration: 1.5,
        stagger: 0.3,
        scrollTrigger: {
          trigger: ".card",
          start: "top 90%",
          end: "top 50%",
          markers: true, // Mostra markers para debug
          scrub: 1, // Animação atada ao scroll
          pin: false,
        },
      });
    </script>
  </body>
</html>
```


## Exemplo Reveal word by word

Split das palavras do text num array para ser aplicado ao GSAP através da class `char`.

```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      }

      .text-section {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding: 50px;
        text-align: center;
        background: #f8f9fa;
      }

      .split-text {
        font-size: 3rem;
        font-weight: bold;
        margin-bottom: 2rem;
        color: #333;
      }

      .char {
        display: inline-block;
        opacity: 0;
        transform: translateY(100px);
      }
    </style>
  </head>

  <section class="text-section">
    <h2 class="split-text">Um relógio para a sua saúde</h2>
  </section>

  <script>
    gsap.registerPlugin(ScrollTrigger);

    // 1. Split text em caracteres
    function splitTextIntoChars(selector) {
      const element = document.querySelector(selector);
      const text = element.textContent;
      element.innerHTML = "";

      text.split(" ").forEach((char) => {
        const span = document.createElement("span");
        span.textContent = char + "\u00A0";
        span.className = "char";
        element.appendChild(span);
      });
    }

    // 2. Aplicar split
    splitTextIntoChars(".split-text");

    // 3. Animar caracteres
    gsap.to(".char", {
      y: 0,
      opacity: 1,
      duration: 0.8,
      stagger: 0.2,
      ease: "power2.out",
      scrollTrigger: {
        trigger: ".split-text",
        start: "top 80%",
      },
    });
  </script>
</html>



```

## Exemplo scroll horizontal

```html

<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
      * {
        margin: 0;
        padding: 0;
      }
      .horizontal-section {
        overflow: hidden;
      }

      .horizontal-wrapper {
        display: flex;
        width: 400vw; /* 4 painéis × 100vw */
      }

      .panel {
        width: 100vw;
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        color: white;
        font-size: 2rem;
      }

      .panel-1 {
        background: linear-gradient(45deg, #667eea, #764ba2);
      }
      .panel-2 {
        background: linear-gradient(45deg, #f093fb, #f5576c);
      }
      .panel-3 {
        background: linear-gradient(45deg, #4facfe, #00f2fe);
      }
      .panel-4 {
        background: linear-gradient(45deg, #43e97b, #38f9d7);
      }

      .spacer {
        height: 100vh;
        background: #f8f9fa;
      }
    </style>
  </head>
  <body>
    <div class="spacer"></div>
    <section class="horizontal-section">
      <div class="horizontal-wrapper">
        <div class="panel panel-1">
          <h2>Painel 1</h2>
          <p>Primeiro painel do slider horizontal</p>
        </div>
        <div class="panel panel-2">
          <h2>Painel 2</h2>
          <p>Segundo painel com cor diferente</p>
        </div>
        <div class="panel panel-3">
          <h2>Painel 3</h2>
          <p>Terceiro painel do conjunto</p>
        </div>
        <div class="panel panel-4">
          <h2>Painel 4</h2>
          <p>Último painel da sequência</p>
        </div>
      </div>
    </section>
    <div class="spacer"></div>

    <script>
      gsap.registerPlugin(ScrollTrigger);

      // Scroll horizontal
      gsap.to(".horizontal-wrapper", {
        x: () =>
          -(
            document.querySelector(".horizontal-wrapper").scrollWidth -
            window.innerWidth
          ) + "px",
        ease: "none",
        scrollTrigger: {
          trigger: ".horizontal-section",
          pin: true,
          scrub: 1,
          end: () =>
            "+=" + document.querySelector(".horizontal-wrapper").scrollWidth,
          anticipatePin: 1,
        },
      });

      // Animações individuais para cada painel
      gsap.utils.toArray(".panel").forEach((panel, i) => {
        gsap.from(panel.querySelector("h2"), {
          y: 100,
          opacity: 0,
          duration: 1,
          scrollTrigger: {
            trigger: panel,
            containerAnimation: gsap.getById("horizontal-scroll"),
            start: "left 80%",
            end: "left 20%",
            toggleActions: "play reverse play reverse",
          },
        });
      });
    </script>
  </body>
</html>

```

## Exemplo de progressos

```html

<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      }

      .progress-section {
        padding: 100px 50px;
        max-width: 800px;
        margin: 0 auto;
      }

      .skill {
        margin: 30px 0;
      }

      .skill-info {
        display: flex;
        justify-content: space-between;
        margin-bottom: 10px;
        font-weight: bold;
      }

      .progress-bar {
        width: 100%;
        height: 10px;
        background: #e2e8f0;
        border-radius: 5px;
        overflow: hidden;
      }

      .progress-fill {
        height: 100%;
        background: linear-gradient(90deg, #667eea, #764ba2);
        width: 0%;
        border-radius: 5px;
        transition: width 2s ease-out;
      }

      .counters {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 30px;
        margin-top: 80px;
        text-align: center;
      }

      .counter-number {
        font-size: 3rem;
        font-weight: bold;
        color: #667eea;
        display: block;
      }

      .counter-label {
        font-size: 1.1rem;
        color: #666;
        margin-top: 10px;
      }
    </style>
  </head>

  <body>
    <section class="progress-section">
      <h2>As minhas competências</h2>

      <div class="skill">
        <div class="skill-info">
          <span>Design</span>
          <span class="percentage">95%</span>
        </div>
        <div class="progress-bar">
          <div class="progress-fill" data-width="95"></div>
        </div>
      </div>

      <div class="skill">
        <div class="skill-info">
          <span>Programação</span>
          <span class="percentage">90%</span>
        </div>
        <div class="progress-bar">
          <div class="progress-fill" data-width="90"></div>
        </div>
      </div>

      <div class="counters">
        <div class="counter-item">
          <div class="counter-number" data-target="150">0</div>
          <div class="counter-label">Projetos</div>
        </div>
        <div class="counter-item">
          <div class="counter-number" data-target="50">0</div>
          <div class="counter-label">Clientes</div>
        </div>
        <div class="counter-item">
          <div class="counter-number" data-target="98">0</div>
          <div class="counter-label">% Satisfação</div>
        </div>
      </div>
    </section>

    <script>
      gsap.registerPlugin(ScrollTrigger);

      // 1. Progress bars animadas
      gsap.utils.toArray(".progress-fill").forEach((bar) => {
        const width = bar.getAttribute("data-width");

        gsap.to(bar, {
          width: width + "%",
          duration: 2,
          ease: "power2.out",
        });
      });

      // 2. Counters animados
      gsap.utils.toArray(".counter-number").forEach((counter) => {
        const target = parseInt(counter.getAttribute("data-target"));

        gsap.to(counter, {
          textContent: target,
          duration: 2.5,
          ease: "power2.out",
          snap: { textContent: 1 },
          onUpdate: function () {
            counter.textContent = Math.ceil(counter.textContent);
          },
        });
      });
    </script>
  </body>
</html>
```

